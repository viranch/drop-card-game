{
  "name": "drop-game",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "boardgame.io": "^0.20.1",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-router-dom": "^4.2.2",
    "react-scripts": "^1.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "server": "npm run build && babel-node --presets zero src/server.js"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-zero": "^0.1.0"
  }
}
